<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Favorite BBQ Blog</title>
  <link rel="stylesheet" href="./stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h1>
        <a class="header" href="/">Radar Menu</a>
      </h1>
      <nav>
        {{#if loggedIn}}
        <a id="nav" href="/dashboard">dashboard</a>
        <button id="nav" class="btn-no-style">logout</button>
        {{else}}
        <a id="nav" href="/login">login</a>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      Thanks for visiting!
    </footer>
  </div>
  
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>